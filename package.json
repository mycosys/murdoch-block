{
  "name": "adblock-murdoch-list",
  "version": "1.0.0",
  "description": "A list of URLs and blocklists for News Limited websites",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist; mkdir dist && npm run build:adblock",
    "build:adblock": "node tools/adblock-plus > dist/adblock-plus.txt"
  },
  "author": "Ash Kyd",
  "license": "ISC",
  "dependencies": {
  }
}
